const menuBtn=document.getElementById('menuBtn');const nav=document.getElementById('nav');menuBtn?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));menuBtn.textContent=open?'×':'☰'});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuBtn?.setAttribute('aria-expanded','false');if(menuBtn)menuBtn.textContent='☰'}));const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.15});document.querySelectorAll('.reveal').forEach(el=>io.observe(el))}else document.querySelectorAll('.reveal').forEach(el=>el.classList.add('in'));const form=document.getElementById('bookingForm');const generated=document.getElementById('generated');const copyBtn=document.getElementById('copyBtn');const status=document.getElementById('status');let message='';form?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const name=(data.get('name')||'').trim();const service=(data.get('service')||'').trim();const time=(data.get('time')||'').trim();const note=(data.get('note')||'').trim();if(!name||!service){status.textContent='Add meg a neved és válassz egy irányt.';return}message=`Szia! ${name} vagyok. ${service.toLowerCase()} miatt szeretnék érdeklődni.${time?` Nekem időpontnak ez lenne ideális: ${time}.`:''}${note?` Röviden az elképzelésem: ${note}`:''} Tudnánk egyeztetni?`;generated.textContent=message;generated.classList.add('show');copyBtn.disabled=false;status.textContent='Az üzenet elkészült. Másold ki, majd nyisd meg a Facebookot.'});copyBtn?.addEventListener('click',async()=>{if(!message)return;try{await navigator.clipboard.writeText(message);status.textContent='Üzenet kimásolva.'}catch(_){const ta=document.createElement('textarea');ta.value=message;document.body.appendChild(ta);ta.select();document.execCommand('copy');ta.remove();status.textContent='Üzenet kimásolva.'}});